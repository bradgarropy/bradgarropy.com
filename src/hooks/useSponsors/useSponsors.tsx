import {Sponsors} from "types/sponsor"

const useSponsors = (): Sponsors => {
    const sponsors = {
        "monthly": [],
        "one-time": [],
    }

    return sponsors
}

export default useSponsors
